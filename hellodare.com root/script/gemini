/* Page Container */
.page-container {
    display: flex;
    height: 100vh; /* Ensure container fills viewport height */
    width: 100vw;
    overflow: hidden; /* Prevent body scroll interfering */
  }
  
  /* ... other styles ... */
  
  .middle-column {
    flex: 3;
    display: flex; /* Keep flex */
    /* align-items and justify-content are less relevant now as the track scrolls */
    overflow: hidden; /* CRUCIAL: Hide parts of video-track not in view */
    height: 100%; /* Take full height of page-container */
    position: relative; /* Needed if adding absolute indicators later */
  }
  
  /* VIDEO PORTFOLIO adjustments */
  .video-track {
    display: flex;
    flex-direction: column;
    /* Remove overflow-y: auto - JS will handle movement */
    /* Remove height: 100% - let it grow naturally */
    width: 100%;
    /* Disable native scrollbars (you already have this) */
    scrollbar-width: none;
    -ms-overflow-style: none;
    /* Add transition for smooth scrolling */
    transition: transform 0.6s ease-in-out; /* Adjust timing & easing */
  }
  
  .video-track::-webkit-scrollbar {
    display: none;
  }
  
  /* Video container */
  .video-item { /* Assuming .video-item wraps each iframe */
    width: 100%;
    height: 100vh; /* CRUCIAL: Each item takes full viewport height */
    flex-shrink: 0; /* Prevent items from shrinking */
    display: flex; /* Optional: center iframe within item */
    align-items: center; /* Optional: center iframe within item */
    justify-content: center; /* Optional: center iframe within item */
    /* background-color: #eee; */ /* Optional: for debugging layout */
  }
  
  .video-item iframe {
    width: 90%; /* Or adjust as needed */
    height: 80%; /* Or adjust as needed */
    max-width: 1280px; /* Example max width */
    max-height: 720px; /* Example max height */
    /* Resetting pointer events - we handle scroll differently now */
    pointer-events: auto;
    border-radius: var(--video-border-radius);
    /* background-color: aqua; */ /* For debugging */
  }
  
  /* Remove the old video-container styles if they conflict */
  /* .video-container { ... } */
  
  /* Ensure iframe interaction isn't blocked if needed,
     but be aware this means scroll won't work while hovering iframe */
  /* .video-item iframe {
      pointer-events: auto;
  } */
  
  /* Remove absolute positioning for controls if they were relative to video-container */
  /* Adjust .video-controls positioning if needed, perhaps relative to .video-item */
  .video-controls {
     position: absolute; /* Maybe position relative to .video-item now */
     bottom: 20px; /* Example */
     left: 50%;
     transform: translateX(-50%);
     z-index: 10;
     /* background: rgba(0,0,0,0.5); */ /* Add background for visibility */
     padding: 5px 10px;
     border-radius: 5px;
     /* Reset mix-blend-mode if it causes issues */
     /* mix-blend-mode: difference; */
  }